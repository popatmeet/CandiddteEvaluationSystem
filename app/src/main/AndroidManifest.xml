<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.candidateevaluationsystemandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan"
        tools:replace="android:icon">
        <activity android:name=".cpdquiz"></activity>
        <activity android:name=".CPDDELAY" />
        <activity android:name=".Result" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".Requirement" />
        <activity android:name=".Candidatelist" />
        <activity android:name=".AdminSide" />
        <activity android:name=".splash" />
        <activity android:name=".Skills_Specialities" />
        <activity android:name=".educational_details" />
        <activity android:name=".Details" />
        <activity android:name=".Home" />
        <activity android:name=".activity_intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>